/**
 * UCHAGUZI SAFI — PostCSS Configuration
 * Processes Tailwind CSS directives and auto-prefixes for
 * cross-browser compatibility (critical for Kenya's diverse
 * mobile browser landscape).
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
